--- src/CoreComponentRepository.php
+++ src/CoreComponentRepository.php
@@ -35,7 +35,7 @@ class CoreComponentRepository
 
     protected static function finalizeRepository($rn) {
         if($rn == "bad" && env('DEMO_MODE') != 'On') {
-            return redirect('https://activeitzone.com/activation/')->send();
+            return null;
         }
     }
 
